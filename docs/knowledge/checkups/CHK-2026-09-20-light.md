# CHK-2026-09-20-light: Stale TODO/doc line fixed; analytics, glossary and feature-flag dimensions clean

## Run info

- **Tier:** light
- **Date:** 2026-09-20
- **Period covered:** 2026-09
- **Dimensions walked:** Unused functionality, Scenario quality, Glossary/naming drift, Doc-reality drift, Feature-flag lifecycle

## Findings

### Scenario quality

- **Finding:** `ChecklistDetailScreen.test.tsx` carried a stale `// TODO: replace with the actual accessibility label once implemented.` comment above a `getByLabelText('Start run')` lookup — the label was implemented (`ChecklistDetailScreen.tsx:153`) and the TODO no longer applied.
- **Debt quadrant:** reckless-inadvertent (a mistake now understood — the comment was never removed once its condition was satisfied).
- **Disposition:** fixed inline (same commit as this write-up).
- **Graduation candidate:** no.

### Doc-reality drift

- **Finding:** `docs/ARCHITECTURE.md`'s "Local simulator scenarios" section said `.maestro/` contains "three local iOS scenarios" (completing a run, confirming/discarding an exit, and swipe-back). The directory now holds seven scenario files — CheL-5 added `run-history.yaml`/`run-history-discard.yaml` and CheL-36 added the two external-run callback flows — without the summary line being updated.
- **Debt quadrant:** prudent-inadvertent (expected doc drift as features shipped; no CI enforcement on this line).
- **Disposition:** fixed inline (same commit as this write-up).
- **Graduation candidate:** no — the exact scenario count/list is documented in `docs/SCENARIOS.md`'s Coverage table, which stays current per-PR; ARCHITECTURE.md's line now deliberately stays qualitative instead of enumerating a count that will drift again.

## Dimensions with nothing to report

- **Unused functionality:** every analytics event and screen view in `docs/ANALYTICS_EVENTS.md` has a firing call site (`grep` cross-check across `src/`); `ts-prune` found no genuinely unused exports (all reported items were internal re-export false positives). No feature-flag-gated entry points exist.
- **Glossary/naming drift:** `docs/GLOSSARY.md` terms and code symbols match; no aliases (`session`, `commitment`, etc.) found creeping into code or docs.
- **Feature-flag lifecycle:** project has no feature-flag/kill-switch mechanism — dimension does not apply.

## Ledger updates

- Cadence & due status: `Light` → Last run `2026-09-20`, Period covered `2026-09`
- Open findings added: none
- Resolved findings closed: none
