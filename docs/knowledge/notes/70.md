# 70: Night mode (dark theme, follows OS setting)

## Notes

- 2026-09-23: CI waits for the full iOS simulator build even on commits that only touch docs (no app code changed) — we should define when a full build/CI run is actually needed vs. when it can be skipped (e.g. docs-only or config-only commits).

## Debrief summary
