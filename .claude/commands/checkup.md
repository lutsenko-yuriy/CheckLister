Run via Bash: `python3 scripts/skill_router skills/manage/checkup/SKILL.md --args '$ARGUMENTS'`
If the script exits non-zero (LM Studio unavailable or model not loaded), fall back to reading `skills/manage/checkup/SKILL.md` and executing it yourself.

$ARGUMENTS
