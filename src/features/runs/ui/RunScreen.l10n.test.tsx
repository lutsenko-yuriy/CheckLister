// Scenario stubs for #80 (WU0). `implement` turns each `it.todo` into a real test
// that performs the TODO steps above it.

describe('RunScreen localization (#80)', () => {
  // TODO:
  // 1. Mock preferred locales as [ru-RU].
  // 2. Start a run of a 2-item checklist and render RunScreen inside I18nProvider.
  // 3. Verify the "Complete the checklist" button label and text use the `ru` value.
  // 4. Spy on Alert.alert and trigger leaving the screen (beforeRemove).
  // 5. Verify the "Are you sure?" title, body and Cancel/Discard buttons use `ru` values.
  it.todo('renders the complete button and discard confirmation in Russian');
});
