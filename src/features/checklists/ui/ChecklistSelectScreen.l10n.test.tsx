// Scenario stubs for #80 (WU0). `implement` turns each `it.todo` into a real test
// that performs the TODO steps above it.

describe('ChecklistSelectScreen localization (#80)', () => {
  // TODO:
  // 1. Mock preferred locales as [fr-FR].
  // 2. Seed 2 checklists and begin an external select session.
  // 3. Render ChecklistSelectScreen inside I18nProvider.
  // 4. Verify the header title and the Cancel label use `fr` values.
  // 5. Verify the row names are unchanged and the item counts use the `fr` plural forms.
  it.todo('renders the picker in French with checklist names verbatim');
});
