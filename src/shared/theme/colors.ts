// Deprecated alias kept only until every screen migrates to `useTheme()` /
// `createThemedStyles()` (N/A-70 WU3 removes this file). Always the light
// palette — components still importing this never react to dark mode.
export { lightPalette as colors } from './palette';
